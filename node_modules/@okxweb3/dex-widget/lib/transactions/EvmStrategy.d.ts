import { EthereumProvider } from '../types';
import { BlockchainStrategy } from './IBlockchainStrategy';
export declare class EvmStrategy implements BlockchainStrategy {
    private iframeWindow;
    constructor(iframeWindow: Window);
    processTransaction(method: string, id: string, path: string, requestArgs: any[], provider: EthereumProvider, type: string): Promise<void>;
    onProviderEvent(event: string, params: unknown): void;
    registerProviderEventListeners(provider: any): void;
}
