import { OkxEventListeners } from './events';
export declare class IframeEventEmitter {
    private contentWindow;
    private eventEmitter;
    private listeners;
    private widgetListener;
    constructor(contentWindow: Window, listeners?: OkxEventListeners);
    stopListeningIframe(): void;
    updateListeners(listeners?: OkxEventListeners): void;
}
